const name = document.getElementById('name').value;
const email = document.getElementById('email').value;
const password = document.getElementById('password').value;

document.getElementById('populatedName').textContent = name;
document.getElementById('populatedEmail').textContent = email;
document.getElementById('populatedPassword').textContent = password;
